import { RegisterForm } from "../components/auth";

const Register = () => {
  return (
    <div className="bg-blue-light h-screen">
      <RegisterForm />
    </div>
  );
};

export default Register;
